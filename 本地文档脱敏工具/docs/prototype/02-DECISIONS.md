# 本地文档脱敏工具 MVP Decision Log

## Effective Decisions

Confirmed by the user on 2026-07-29 with `开干吧`.

| ID | Date | Decision | Reason | Affected surfaces |
|---|---|---|---|---|
| D-001 | 2026-07-29 | 在当前会话和 `D:\GPT项目\通用\本地文档脱敏工具` 独立目录继续 | 保留讨论上下文，同时隔离项目文件 | 全项目 |
| D-002 | 2026-07-29 | 先确认需求、安全和验收基线，再写业务代码 | 避免把安全假设直接实现为事实 | 工作流 |
| D-003 | 2026-07-29 | 产品定位为“本地 AI 分析副本生成器” | 技术脱敏不等于解密、脱密或外发审批 | 文案、状态、报告 |
| D-004 | 2026-07-29 | 使用 Python + PySide6 桌面界面，OOXML 扫描、识别、转换、导出和 UI 分层 | Windows 离线部署成熟，格式安全逻辑可独立测试 | 架构、构建 |
| D-005 | 2026-07-29 | 首版离线识别栈采用 RapidOCR/PP-OCRv6 small、ONNX Runtime CPU、spaCy 中文模型+EntityRuler、OpenCV QR/内置 Haar 人脸候选和本地规则词库；进程级网络审计钩子阻断连接且不提供运行时下载 | 覆盖图片文字、中文实体、二维码、人脸和确定性字段，同时控制包体积 | 识别、构建 |
| D-006 | 2026-07-29 | 首版同时支持 `.docx` 和 `.xlsx`，输出与输入同格式的 AI 分析副本 | 直接满足用户对 Word/Excel 的最低要求 | 格式适配器 |
| D-007 | 2026-07-29 | 原文件只读；先清点全部 OOXML 部件/关系，再从空白 DOCX/XLSX 允许清单重建 | 不能把高层库“恰好丢失对象”当作安全清理 | 输入、导出 |
| D-008 | 2026-07-29 | 默认平衡模式，另提供严格模式；按类别采用一致别名、粒度泛化、区间化和结构保持模拟值 | 保留业务语义，避免全文变成无差别占位符 | 复核、预览、导出 |
| D-009 | 2026-07-29 | 所有图片、绘图和视觉对象必须进入清单；OCR/QR/人脸/印章/签名只给候选，用户逐图决定后才能导出 | 通用模型无法可靠自动覆盖所有印章、签名和照片 | 图片复核 |
| D-010 | 2026-07-29 | 图片局部处理必须改写真实像素、去元数据并重新编码；不复制原媒体字节 | 防止遮罩可移除和图像元数据泄漏 | 图片导出、复扫 |
| D-011 | 2026-07-29 | Word 保留标题、段落、列表、表格和基本样式；修订输出最终可见状态，隐藏内容/批注可脱敏后转成可见附注或移除 | 在安全重建和可读性之间取平衡 | DOCX 导出 |
| D-012 | 2026-07-29 | Excel 保留单元格结构、合并、宽高、基本样式和数值类型；公式默认输出上次保存的显示值，所有输出区域可见 | 避免公式、外链和隐藏区域继续泄漏或执行 | XLSX 导出 |
| D-013 | 2026-07-29 | 活动链接、外部关系、宏、ActiveX、OLE、远程模板、数据连接和无法安全解析的对象不得复制 | 这些对象扩大联网、执行和隐藏数据风险 | 对象复核、导出 |
| D-014 | 2026-07-29 | 每个任务生成密码加密的 XLSX 映射表，使用 Office Agile Encryption；密码不保存，映射表与 AI 副本分目录 | 满足后续对应还原，同时降低映射表误上传风险 | 导出、完成页 |
| D-015 | 2026-07-29 | 不集成网络、云模型、上传、剪贴板复制、自动更新或运行时模型下载；依赖和模型固定哈希 | 保持明确的离线信任边界 | 全项目 |
| D-016 | 2026-07-29 | 导出后独立解包并复扫实际副本、映射表加密状态和报告；失败时不得显示完成 | 防止只检查预览而漏掉真实输出 | 安全检查 |
| D-017 | 2026-07-29 | 项目交付为 PyInstaller `onedir` Windows 绿色版；用户任务输出分为 `AI交付` 与 `本地保管` | 便于离线运行、审计和避免误传 | 构建、交付 |
| D-018 | 2026-07-29 | 四步界面改为 `选文件 → 自动检查 → 确认处理 → 生成文件`，每屏一个主任务；第 3 步采用待办列表与当前卡片 | 用户明确反馈原界面太复杂、操作不清楚 | 全局界面、复核 |
| D-019 | 2026-07-29 | 增加 `采用全部普通建议`；普通文字和结果确定的本地规则可批量处理，图片、低置信度未知项、组合风险和对象仍需人工确认 | 同时满足一键操作与原有安全边界 | 确认处理 |
| D-020 | 2026-07-29 | 增加 Windows 当前用户级加密的本地规则库，区分固定替换与强制判断标准，支持手工维护和 XLSX/CSV 导入 | 用户要求规则持续积累而非任务结束即丢失 | 规则库、检测 |
| D-021 | 2026-07-29 | 命中用户强制规则的内容必须使用代号、星号形式或删除，不能保留原文；工具仍不自行认定法律或制度意义上的密级 | 用户要求所有纳入涉密标准的内容统一转代号，同时保持产品能力边界 | 规则、复核、导出 |
| D-022 | 2026-08-07 | 时间类暂不进入自动识别和替换候选，也不参与组合风险计算；原文时间保持不变 | 用户明确要求“时间类的先不用识别替换”，同时保留事件顺序和分析可读性 | 检测、复核、预览、导出 |
| D-023 | 2026-08-07 | 个人默认规则库中的警种和公安部门使用大写拼音首字母；撞码时改用正式全称的首字母组合 | 延续用户常用 `GA`、`WJ`、`JZ`、`QB` 风格，同时避免不同警种共用同一代号 | 本机规则库 |
| D-024 | 2026-08-07 | 常用个人敏感字段采用可读星号掩码，不再默认改写为“人员甲”“手机号甲”等代号；姓名识别复姓，银行卡仅在明确银行卡语境处理 | 用户给出明确的脱敏输出标准，既保留必要结构，又避免完整原值留存 | 自动建议、预览、导出和映射表 |
| D-025 | 2026-08-07 | 本工具采用静态脱敏副本模型：内置类型规则、公安行业固定词规则、用户强制规则和人工复核分层生效；不引入数据库动态展示白名单 | DataWorks 的场景化规则思想适合复用，但本工具输出的是离线 Office 副本，不存在按查询用户实时展示原文的安全边界 | 规则体系和导出流程 |
| D-026 | 2026-08-07 | 撤销固定内部密钥、明文决定快照和明文任务历史；映射表恢复为用户设置且不保存的强密码 | 原始敏感值、替换关系、文件名和完整路径不得在加密映射表之外持久化 | 本地存储、导出、最近任务 |
| D-027 | 2026-08-07 | 保留已确认项目可进入第 4 步的修复，但未确认项目继续失败关闭；长期规则保存后重置任务并要求重新检查 | 防止步骤卡死，同时保证同一任务只使用一份冻结规则快照 | 第 3 至第 4 步、规则保存 |
| D-028 | 2026-08-07 | 规则库显示系统内置识别、公安行业与地名预置、我的规则；预置规则允许停用，不允许编辑和删除 | 既提供开箱即用的专业规则，又避免用户误改基础预置；自定义规则仍由用户完全控制 | 规则库界面、加密存储 |
| D-029 | 2026-08-07 | 公安行业规则统一使用大写拼音首字母并在碰撞时使用正式全称组合；省级地名预置覆盖全国，海南、福建、广东固定为 HN、FJ、GD | 延续用户习惯并保证跨文档一致；长地名优先于短地名 | 行业规则、地名规则 |
| D-030 | 2026-08-07 | 文件属性和隐藏对象改为系统自动移除并只展示处理结果，不再要求逐项确认；图片仍保留人工确认 | 用户明确要求减少无意义确认，同时图片识别仍存在漏检风险 | 隐藏内容、步骤完成条件 |
| D-031 | 2026-08-07 | 系统内置识别改为完整只读清单展示；判断标准覆盖个人信息、账号网络、业务对象、密码密钥和隐藏内容 | 用户需要知道系统已经支持什么，即使这些规则不能编辑 | 规则库界面 |
| D-032 | 2026-08-07 | 带明确标签的密码、密钥和令牌自动删除且不进入映射表；地点模型候选增加通用行政表达反例过滤 | 密码不应以掩码形式留存，宽泛地名误报不应转嫁给用户 | 检测、转换、映射表 |
| D-033 | 2026-08-07 | 生成文件页取消密码输入，映射表改为 `本地保管` 下可直接打开的 XLSX；继续与 `AI交付` 隔离并显示严禁上传 | 用户最新明确要求去掉密码，替换 D-014 和 D-026 中的映射表密码要求 | 导出、完成页 |
| D-034 | 2026-08-07 | 合并 BUGFIX 两轮中的修改框默认展开、删除状态修复、规则保存后自动重扫和 DOCX 格式保留；不恢复固定密钥、明文决定快照或明文任务历史 | 合并有效体验修正，同时避免重新引入高风险持久化 | UI、规则、导出、本地存储 |
| D-035 | 2026-08-07 | DOCX 导出改为保留原样式结构的原位替换和安全清理，不再从空白 Word 重建 | 用户明确要求保留原文格式 | DOCX 导出 |
| D-036 | 2026-08-07 | 常用判断标准与行业预置写入本机加密规则库一次，之后由用户完全管理 | 解决判断标准空白和预置删除后复活 | 规则库 |
| D-037 | 2026-08-07 | 正在处理的任务允许规则增量应用，仅重新打开新命中或冲突项 | 用户不接受每次从头扫描 | 扫描、复核、规则快照 |
| D-038 | 2026-08-07 | 恢复历史能力，但改为 DPAPI 加密的最小元数据历史，不保存原文和决策明细 | 满足追踪需求并与本地脱敏边界兼容 | 首页、本地存储 |
| D-039 | 2026-08-07 | 文件夹使用顺序队列逐文件复核，不因批量模式降低单文件安全门槛 | 兼顾批量效率和失败关闭 | 选择、队列、汇总 |
| D-040 | 2026-08-07 | 图片复核只显示人脸、结构化敏感值和强制规则命中；普通图片自动净化 | 减少普通 OCR 和景物误报 | 图片识别、复核、导出 |
| D-041 | 2026-08-07 | 指定用户 Word 作为本轮本地回归样本，测试不复制该文件进仓库 | 真实复现图片残留错误且不泄漏用户文档 | 验收 |

## Replaced

| Old rule | Replaced by | Reason |
|---|---|---|
| 首版输入 TXT、DOCX、PDF | D-006 | 用户明确要求至少支持 Excel/Word；首版聚焦 DOCX/XLSX |
| D-014 / D-026 的映射表强密码要求 | D-033 | 用户最新明确要求取消密码输入；映射表改为本地敏感文件直接生成并继续隔离 |
| D-007 “从空白 DOCX 重建” | D-035 | 用户明确要求保留原文完整格式；改为保留格式结构并严格清理危险部件 |
| D-026 / D-034 禁止任务历史 | D-038 | 最新用户要求保留历史；仅允许 DPAPI 加密的最小元数据 |
| D-027 / D-034 规则变更后完整重扫 | D-037 | 改为已解析内容上的增量规则应用 |
| 首版不支持 OCR、印章、二维码和图片文字 | D-005 / D-009 / D-010 | 用户明确要求图片内容进入识别和脱敏 |
| 图片全部移除、不进入副本 | D-009 / D-010 | 用户要求脱敏后仍能理解内容；经像素处理和复核的图片可以保留 |
| 映射只在内存中，不落盘 | D-014 | 用户明确要求生成映射表用于后续还原 |
| 统一用 `[类别_001]` 替换 | D-008 | 会破坏阅读和分析语义，改为按类别最小必要变换 |
| “自动检测无命中即可上传” | D-003 / D-009 / D-016 | 模型会漏检、图片必须人工复核，外发仍受制度约束 |

## Pending

Pending items are not implementation facts.

| ID | Question | Current safe handling | Owner/trigger |
|---|---|---|---|
| P-001 | 是否在第二阶段加入更专门的印章/版面模型 | 首版以 OCR、颜色/轮廓启发式和每图必审为准，不下载大型运行时 | 内部夹具证明首版候选不足时 |
| P-002 | 是否扩展 PDF、PPT、旧式 Office 或宏格式 | 不实现；先完成 DOCX/XLSX 安全闭环 | 用户提出新范围时 |
| P-003 | 是否自动加载映射表并还原 AI 返回的文档 | 首版只生成本地保管映射表，避免未确认的全局替换风险 | 用户明确提出自动还原规则时 |
