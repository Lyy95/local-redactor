# Codex 成本控制规则

这些规则适用于本目录下的原型、前端和产品设计任务。目标是先完成最小可用闭环，再按证据做一次集中验证，避免长时间循环和无边界探索。

## 默认模型与推理

- 普通原型、UI 调整、文案修改、样式修复默认使用低成本模型或中等推理档；不要默认使用最高档模型或 `ultra`。
- 只有用户明确要求“最高质量/深度研究/复杂疑难排查”，或已有证据表明低档无法完成时，才升级模型或推理档，并在升级前说明预期收益。
- 不要因为任务包含浏览器、图片或多个文件，就自动升级到最高档。

## 阶段与预算

- 原型任务拆成四个阶段：确认范围 → 最小实现 → 一次集中回归 → 交付说明。每阶段完成后停止扩展，不自行进入下一轮视觉探索。
- 方向未确认时最多提供 2 个视觉方案；方向确认后停止继续生成方案，直接实现已选方案。
- 单个用户反馈轮次默认控制在 50 万 token 内；预计会超过 100 万 token 时，先停止并汇报消耗、剩余工作和下一步，不继续盲跑。
- 连续两个验证轮次没有产生可见改进时，停止循环，报告阻塞点。
- 用户说“怎么那么慢”“先停”“不要继续”时立即停止当前长流程，只保留已完成结果。

## 浏览器、截图与图片

- 浏览器只做必要的 DOM/交互验证；单个反馈轮次最多 8 次浏览器操作、2 个关键状态截图。
- 优先读取 DOM、文本和选择器，不要反复读取整页截图或把截图的原始数据带入后续上下文。
- 浏览器连接或工具失败最多重试 2 次；仍失败就汇报，不得反复重连、重复读取完整说明。
- 新 UI 原型最多生成 3 张候选图；已有可用方向后禁止继续批量生成图片。
- 不把整张截图、base64、长工具输出或完整历史重复复制到下一轮；只保留与当前修改直接相关的摘要。

## 上下文与工具调用

- 每次工具调用前先判断是否能用一次批量读取、一次补丁或一次回归替代多次往返。
- 同一规则、说明、文件或浏览器文档只读取一次；后续复用已读结果。
- 不为了“看起来完整”运行与验收标准无关的命令、截图、网页搜索或图片生成。
- 发现上下文已明显膨胀或发生第二次压缩时，停止追加历史；用 5～10 条要点建立新阶段摘要，必要时新开任务继续。
- 用户提供多条批注时合并为一次实现和一次统一回归，不逐条启动独立长流程。

## 变更边界

- 只实现用户当前确认的范围，保留已有功能和受保护源文件。
- 诊断、解释或评审请求默认只读，不顺手修改代码。
- 外部发布、部署、购买、删除等高成本或外部状态变更必须单独获得用户授权。
